<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.laks.tvseries.featuresettings">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_featuresettings">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".language.LanguageActivity"></activity>
        <activity android:name=".about.AboutActivity"></activity>
        <activity android:name=".about.TermOfUseActivity"></activity>
    </application>

</manifest>